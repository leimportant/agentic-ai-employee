Berikut adalah contoh kode untuk landing page hero section dan pricing table:
```tsx
import { Button, GradientText } from 'shadcn/ui';
import { Lucide } from 'lucide-react';
import { useState } from 'react';

const HeroSection = () => {
  return (
    <div className="h-screen flex items-center justify-center bg-slate-900">
      <div className="text-center">
        <GradientText
          className="text-5xl font-bold mb-4"
          from="bg-indigo-500"
          to="bg-violet-500"
        >
          Karyawan AI untuk Bisnis Kamu
        </GradientText>
        <p className="text-slate-50 text-2xl mb-8">
          Otomasi customer service & sales 24/7. Setup 5 menit, mulai gratis.
        </p>
        <div className="flex justify-center space-x-4">
          <Button
            className="bg-amber-500 hover:bg-amber-600 text-slate-50 py-3 px-6 rounded"
            onClick={() => console.log('Mulai Gratis')}
          >
            Mulai Gratis
          </Button>
          <Button
            className="border border-slate-50 text-slate-50 py-3 px-6 rounded hover:bg-slate-700"
            onClick={() => console.log('Lihat Demo')}
          >
            Lihat Demo
          </Button>
        </div>
      </div>
    </div>
  );
};

const PricingTable = () => {
  return (
    <div className="bg-slate-800 p-6 rounded">
      <h2 className="text-slate-50 text-3xl font-bold mb-4">Pricing</h2>
      <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
        <div className="bg-slate-700 p-6 rounded">
          <h3 className="text-slate-50 text-2xl font-bold mb-2">Free</h3>
          <p className="text-slate-400 text-xl mb-4">1 AI agent, 100 pesan/bulan, 1 user, Web chat only</p>
          <Button
            className="bg-amber-500 hover:bg-amber-600 text-slate-50 py-3 px-6 rounded"
            onClick={() => console.log('Mulai Gratis')}
          >
            Mulai Gratis
          </Button>
        </div>
        <div className="bg-slate-700 p-6 rounded">
          <h3 className="text-slate-50 text-2xl font-bold mb-2">Starter</h3>
          <p className="text-slate-400 text-xl mb-4">3 agents, 5000 pesan, 5 users, WA + Telegram</p>
          <Button
            className="bg-indigo-500 hover:bg-indigo-600 text-slate-50 py-3 px-6 rounded"
            onClick={() => console.log('Pilih Starter')}
          >
            Pilih Starter
          </Button>
          <span className="text-slate-50 text-sm ml-2">POPULAR</span>
        </div>
        <div className="bg-slate-700 p-6 rounded">
          <h3 className="text-slate-50 text-2xl font-bold mb-2">Pro</h3>
          <p className="text-slate-400 text-xl mb-4">Unlimited semua, priority support, custom AI</p>
          <Button
            className="bg-indigo-500 hover:bg-indigo-600 text-slate-50 py-3 px-6 rounded"
            onClick={() => console.log('Pilih Pro')}
          >
            Pilih Pro
          </Button>
        </div>
      </div>
    </div>
  );
};

const App = () => {
  return (
    <div className="bg-slate-900">
      <HeroSection />
      <PricingTable />
    </div>
  );
};

export default App;
```
Contoh di atas menggunakan komponen `shadcn/ui` dan `lucide-react` untuk membuat hero section dan pricing table yang responsif dan dapat diakses. Anda dapat menyesuaikan kode di atas untuk memenuhi kebutuhan Anda.