```frontend/app/pages/index.tsx
import { useState } from 'react';
import { Dialog, Button } from 'shadcn/ui';
import { AiOutlineVideoCamera } from 'react-icons/ai';
import Link from 'next/link';

export default function Home() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <div className="h-screen flex flex-col justify-center items-center bg-slate-900">
      <div className="text-center">
        <h1 className="text-5xl font-bold gradient-text mb-4">
          Karyawan AI untuk Bisnis Kamu
        </h1>
        <p className="text-2xl text-slate-400 mb-8">
          Otomasi customer service & sales 24/7. Setup 5 menit, mulai gratis.
        </p>
        <div className="flex justify-center space-x-4">
          <Button
            className="bg-amber-500 hover:bg-amber-600"
            onClick={() => console.log('Mulai Gratis')}
          >
            Mulai Gratis
          </Button>
          <Button
            className="bg-slate-800 hover:bg-slate-700 border border-slate-600"
            onClick={() => setIsOpen(true)}
          >
            Lihat Demo
          </Button>
        </div>
      </div>
      <Dialog isOpen={isOpen} onClose={() => setIsOpen(false)}>
        <Dialog.Title>
          <AiOutlineVideoCamera size={24} />
          Lihat Demo
        </Dialog.Title>
        <Dialog.Description>
          Silakan lihat demo kami untuk melihat lebih lanjut tentang fitur-fitur kami.
        </Dialog.Description>
      </Dialog>
    </div>
  );
}
```

```frontend/app/components/PricingTable.tsx
import { useState } from 'react';
import { Table, Badge } from 'shadcn/ui';

export default function PricingTable() {
  const [activeTab, setActiveTab] = useState('monthly');

  return (
    <div className="bg-slate-800 p-6 rounded-lg">
      <h2 className="text-3xl font-bold mb-4">Pricing</h2>
      <div className="flex justify-center mb-4">
        <button
          className={`${
            activeTab === 'monthly' ? 'bg-indigo-500 text-slate-50' : 'bg-slate-700 text-slate-400'
          } py-2 px-4 rounded-lg mr-2`}
          onClick={() => setActiveTab('monthly')}
        >
          Bulanan
        </button>
        <button
          className={`${
            activeTab === 'yearly' ? 'bg-indigo-500 text-slate-50' : 'bg-slate-700 text-slate-400'
          } py-2 px-4 rounded-lg`}
          onClick={() => setActiveTab('yearly')}
        >
          Tahunan
        </button>
      </div>
      <Table>
        <Table.Head>
          <Table.Row>
            <Table.Cell>Paket</Table.Cell>
            <Table.Cell>Fitur</Table.Cell>
            <Table.Cell>Harga</Table.Cell>
          </Table.Row>
        </Table.Head>
        <Table.Body>
          <Table.Row>
            <Table.Cell>Gratis</Table.Cell>
            <Table.Cell>1 AI agent, 100 pesan/bulan, 1 user, Web chat only</Table.Cell>
            <Table.Cell>
              <Button className="bg-amber-500 hover:bg-amber-600">Mulai Gratis</Button>
            </Table.Cell>
          </Table.Row>
          <Table.Row>
            <Table.Cell>Starter</Table.Cell>
            <Table.Cell>3 agents, 5000 pesan, 5 users, WA + Telegram</Table.Cell>
            <Table.Cell>
              <div className="flex items-center">
                <span className="text-2xl font-bold">Rp 99.000/bln</span>
                <Badge className="ml-2 bg-indigo-500 text-slate-50">POPULAR</Badge>
              </div>
              <Button className="bg-amber-500 hover:bg-amber-600 mt-2">Pilih Starter</Button>
            </Table.Cell>
          </Table.Row>
          <Table.Row>
            <Table.Cell>Pro</Table.Cell>
            <Table.Cell>Unlimited semua, priority support, custom AI</Table.Cell>
            <Table.Cell>
              <span className="text-2xl font-bold">Rp 299.000/bln</span>
              <Button className="bg-amber-500 hover:bg-amber-600 mt-2">Pilih Pro</Button>
            </Table.Cell>
          </Table.Row>
        </Table.Body>
      </Table>
    </div>
  );
}
```

```frontend/app/pages/index.tsx (lanjutan)
import PricingTable from '../components/PricingTable';

export default function Home() {
  // ...
  return (
    <div>
      {/* Hero Section */}
      <div className="h-screen flex flex-col justify-center items-center bg-slate-900">
        {/* ... */}
      </div>
      {/* Pricing Table */}
      <PricingTable />
    </div>
  );
}
```