```frontend/app/(auth)/login/page.tsx
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { GoogleIcon } from "@/components/ui/icons";
import Link from 'next/link';
import { useState } from "react";
import { useRouter } from 'next/router';

const LoginPage = () => {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const router = useRouter();

  const handleGoogleAuth = async () => {
    // Implementasi autentikasi google
    router.push('/dashboard');
  };

  return (
    <Card className="max-w-md mx-auto mt-20 p-4">
      <CardHeader>
        <CardTitle>Masuk</CardTitle>
      </CardHeader>
      <CardContent>
        <Input
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          placeholder="Email"
          className="mb-4"
        />
        <Input
          type="password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          placeholder="Password"
          className="mb-4"
        />
        <Button className="bg-indigo-500 hover:bg-indigo-700 mb-4" onClick={handleGoogleAuth}>
          <GoogleIcon className="mr-2" />
          Masuk dengan Google
        </Button>
        <p className="text-slate-400 text-center">
          Belum memiliki akun? <Link href="/register"><a className="text-indigo-500 hover:text-indigo-700">Daftar</a></Link>
        </p>
      </CardContent>
    </Card>
  );
};

export default LoginPage;
```